<h1>Messages</h1>
<div class="row">
  <div class="bg-info col-6">
    <div class="p-2">
      <%= form_with url: "/messages", method: :post do |f| %>
        <%= f.text_field :text, placeholder: "Введите текст сообщения" %>
        <%= f.submit "create" %>
      <% end %>
      <% @messages.each do |message| %>
        <%= message.text %><br>
      <% end %>
    </div>
  </div>

  <div class="bg-success col-6">
    <div class="p-2">
      <%= form_with url: send_message_path do |f| %>
        <h4>Задать расписание показа сообщений</h4>
        <label for="score">количество показав 1-5:</label>
        <%= f.number_field :score, min: 1, max: 5, value: 2 %>
        <div> задается в количестве секунд от данного момента</div>
        <div class="score_show">
          <%= f.number_field :score_show, class: "score_show", value: 5%>
        </div>
        <div>количество сообщений (за 1 показ)</div>
          <%= f.number_field :messages, value: 3 %>
        <div>с интервалом (задается в секундах, если оставить пустым то, от 1 до 10 )</div>
          <%= f.number_field :interval, value: 2 %>
        <div>url отправки (если null то берем из .env) </div>
          <%= f.text_field :uri, class: "mb-2" %>
        <div class="m-2">
          <%= f.submit "send messages", class: "btn btn-dark"%>
        </div>
      <% end%>
    </div>
  </div>
</div>
